---
layout: post
title: 2023-2024数据库2A卷
date: 2026-07-01 22:19:36
categories:
  - 期末复习
category_path: 期末复习/数据库原理
description: 描述
keywords: 标签
---
   
# 数据库原理与应用 考试试卷



---

## 一、 解答题（第 1A、1B 中任选一题，第 2 题必做；每小题 9 分，本题共 18 分）

**1A.** 数据库的三级模式结构分别是指什么？以 MySales 数据库为例，举例说明该数据库如何创建和使用三级模式结构。

**1B.** MySQL 数据库中实现并发控制的主要机制有哪些？在多用户环境下，如果一个用户正在向订单表和订单明细表中插入某个客户的销售订单记录，而另一个用户此时在客户表中删除这个客户，阐述如何使用并发控制机制来防止数据的不一致性。

**2.** 设关系模式 $R(A, B, C, D, E)$，根据语义有如下函数依赖集：$F = \{D \rightarrow C, CE \rightarrow B, BC \rightarrow D, DE \rightarrow A\}$。
1. 计算 $(CE)_F^+$ 闭包值；
2. 求关系 $R$ 的候选码；
3. 关系模式 $R$ 最高属于第几范式？

---

## 二、 关系代数运算题（任选 2 小题解答，本题共 12 分）

已知一个学生选课数据库包含学生、课程、教师、选课和授课等 5 个关系模式，分别用 Students, Courses, Teachers, StudCourses, Instructions 表示。各个关系模式表示如下：
* **Students** (Sno, Sname, Gender, Class, Major) = 学生（学号，姓名，性别，班级，所属专业）
* **Courses** (Cno, Cname, type, Credit) = 课程（课程编号，课程名称，课程类型，学分）
* **Teachers** (Tno, Tname, birthdate, gender, Title, Major) = 教师（教师编号，姓名，出生日期，性别，职称，所属专业）
* **StudCourses** (Sno, Cno, Period, Grade) = 选课（学号，课程编号，选课学期，成绩）
* **Instructions** (Tno, Cno, Period) = 授课（教师编码，课程编号，授课学期）

假设同一学生同一学期同一课程只有一个授课教师；课程类型分为“必修”与“选修”两类；教授职称分为教授、副教授和讲师三个等级，试用关系代数完成下列查询。

**1.** 检索 2022-2023-1 学期“达尔文”和“牛顿”这两位老师授课的“数据库原理与应用”这门课程中有哪些学生考试成绩是及格的，列出这些学生的学号、姓名、班级、专业和成绩。（5 分）

**2.** 检索哪些学生 2022-2023-1 学期所有“必修”类课程考试成绩都是及格的，而且没有一门课程是因为之前不及格而重修选课的。（7 分）

**3.** 检索在学号为 s1 这个学生所在的班级中，哪些学生至少选读了 s1 这个学生的全部课程，而且比 s1 这个学生还多选了“人工智能基础”这门课程，列出这些学生的学号和姓名。（7 分）

<div style="text-align: center; margin-top: 20px;">第 1 页</div>

---

## 三、 数据库设计题（本题共 36 分）

已知“美团外卖”网上订餐平台数据库至少包含顾客、骑手、站点、餐馆、菜品等实体，部分实体的主要属性及语义如下：
1. 该网上订餐平台包含多个美团服务站点，每个美团站点包含站点编码、名称、所在城市、地址、负责人等属性；每个站点可以有多个骑手（快递员），每个骑手包含员工编码、姓名、身份证号、手机号码、居住地址等属性。一个骑手在同一时间内只能属于一个美团站点，但在不同时间中可以加入不同美团站点。
2. 一个美团站点可以有多个加盟餐馆，一个餐馆同一年度只能与一个美团站点签订加盟协议；餐馆包含餐馆编码、地址、联系人、联系电话、营业执照编号等属性。
3. 一个餐馆可以提供多个菜品，一个菜品只能由一个餐馆提供，不同餐馆的可以有名称相同的菜品；每个菜品包含菜品编码、名称、主料、口味、报价等属性。
4. 顾客包含顾客账号、姓名、联系电话等属性，顾客有一个默认的送货地址，但在不同时间下单可以有不同送货地址；顾客下单时，需要记录下单时间和选择到货的时间（一般是一个区间值），系统产生一个独一无二的订单编号，顾客在不同时间订购同一个菜品时，其购买单价可能不同。
5. 平台按下单的订单编号分配骑手进行配送，每个骑手每天可接单不超过 100 件；每次配送需要记录其配送费用和配送完成时间，并对骑手服务和餐馆菜品品质进行评价。

试根据上述语义完成下列各题。

**1.** 设计满足上述语义要求的 E-R 图，需标明实体的属性、实体之间的联系以及联系产生的新属性。（12 分）

**2.** 将该 E-R 图转换成关系模式，并指出每一个关系模式中的主码和外码。（10 分）

**3.** 使用 MySQL 建表语句创建“骑手”这个关系模式的数据表，设置其主键和外键等约束条件；同时添加 2 个计算列，要求根据“骑手”的身份证号，自动计算得到“骑手”的出生日期（date 型，例如 1986-02-10）和“性别”（采用“男”或“女”两种值）这两列的值。（6 分）

**4.** “每个骑手每天可接单不超过 100 件、一个餐馆同一年度只能与一个美团站点签订加盟协议”，阐述在 MySQL 数据库设计时可使用什么机制去实现上述两个语义规定。（4 分）

**5.** 在 MySQL 中如何根据“骑手”的出生日期得到其实际年龄？如何根据身份证号的前 4 个字符（例如 3301），通过与地区表（Areas）相连接得到快递员身份证号所对应的那个城市的名称？使得在每次数据检索时可以直接提取年龄和城市这两个列的值，而无需每次计算或多表连接得到这两个列的值。（4 分）

---

## 四、 SQL 程序设计题（本题共 34 分）

**1.** 将下列关系代数转换为一条 SQL 语句。（7 分）

$$R = \Pi_{\text{OrderID}}(\sigma_{\text{Orderdate} >= \text{'2018-01-01'} \wedge \text{Orderdate} <= \text{'2018-06-30'}}(\text{Orders}) \bowtie \text{OrderItems} \bowtie \sigma_{\text{Productname}=\text{'青岛啤酒'}}(\text{Products}) \cap \sigma_{\text{Orderdate} >= \text{'2018-01-01'} \wedge \text{Orderdate} <= \text{'2018-06-30'}}(\text{Orders}) \bowtie \text{OrderItems} \bowtie \sigma_{\text{Productname}=\text{'百威啤酒'}}(\text{Products}))$$

$$\Pi_{\text{CustomerID, Companyname}}(\text{Customers}) - \Pi_{\text{CustomerID, Companyname}}(R \bowtie \text{Customers})$$


**2.** 已知本试卷第二页上“关系代数运算题”中所给出的 5 个关系模式：Students, Courses, Teachers, StudCourses, Instructions。根据第三页图片中所展现的实际数据表结构与字段名（注：字段名以图片表格的英文表头为准，如学号为 `StudentID`、姓名为 `Name`、性别为 `Gender`、出生日期为 `Birthdate`、班级代码为 `ClassID` 等；课程表中包含 `CourseID`, `CourseTitle`, `Credit`, `Category`, `Type` 等；教师表中包含 `TeacherID`, `Name`, `Gender`, `Title`, `Birthdate` 等；班级选课开课表为 `Classno`，包含 `Classno`, `TeacherID`, `Period`, `CourseID`；学生选课成绩表为 `studentid`，包含 `studentid`, `classno`, `score`, `level`, `Period`, `CourseID` 等），请编写对应的 SQL 语句或存储程序完成下列要求。（共 27 分）

1. **数据清洗与规范化：** 编写 SQL 语句，删除所有课程的中文名称（`CourseTitle`）中汉字与非汉字之间的空格。例如：将 `"数据库原理与应用 2"` 修改为 `"数据库原理与应用2"`，将 `"ERP 沙盘模拟"` 修改为 `"ERP沙盘模拟"`，但 `"Java Web 开发框架"` 中 `"Java"` 与 `"Web"` 之间的空格不能删除。（5 分）

2. **数据检索：** 检索哪些学生 2021-2022-1 这个学期所有课程成绩都是及格的，而且“数据库原理与应用 1”这门课程的成绩在其选课班级中排名前 30%。（7 分）

3. **高级程序设计：** 1. 创建一个用户自定义函数（UDF），输入一个学号（`StudentID`），返回这个学生的综合智育成绩的绩点值。绩点值的计算公式如下：
      $$\text{综合智育成绩绩点} = \frac{\sum(\text{每门课程的成绩等级分} \times \text{课程学分})}{\sum \text{所有选读课程学分}}$$
      其中成绩等级分规定如下：0~59 分为 0，60~64 分为 1，65~69 分为 1.5，70~74 分为 2，75~79 分为 2.5，80~84 分为 3，85~89 分为 3.5，90~94 分为 4，95~99 分为 4.5，100 分为 5。同一门课程重修时取分数高的那一次选课成绩。（10 分）
   4. 调用这个用户自定义函数，检索每个学生综合智育成绩在所在专业年级中的排名名次。（5 分）

<div style="text-align: center; margin-top: 20px;">第 3 页</div>